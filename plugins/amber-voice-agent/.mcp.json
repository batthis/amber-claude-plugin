{
  "mcpServers": {
    "amber-voice": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server.js"],
      "env": {
        "AMBER_BRIDGE_URL": "${user_config.bridge_url}",
        "BRIDGE_API_TOKEN": "${user_config.bridge_api_token}"
      }
    }
  }
}
